fix(multiclass+xp): 修好兼职前置判定；兼职并入升级向导，去掉独立升级面板

- fix(兼职): 前置判定错用了「属性修正 >= 3」（等于属性 >= 16），5e 规则是「属性值 >= 13」；
  实测敏捷 13 曾直接失败，现已可正常兼职（并补齐新职业 1 级特性、返回熟练加值/前置说明/多施法职业提醒）
- feat(UI): 角色卡里的「升级 / 兼职」合并成一个向导，两个页签：提升本职（原升级流程）/ 兼职新职业（带前置校验与提示）
- refactor(UI): 移除独立的「升级」面板与旧的 Multiclass 组件；经验满了改为顶栏按钮 + 角色卡内提示
- test: 冒烟新增兼职前置断言（敏捷+1 可兼、智力+0 拒绝、已有本职不达标也拒绝），并同步捕获表
